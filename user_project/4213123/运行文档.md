# 运行文档

## 环境准备

### 必需软件

1. **Node.js** (>= 16.0.0)
   - 下载地址：https://nodejs.org/
   - 安装后验证：`node -v`

2. **MySQL** (>= 8.0)
   - 下载地址：https://dev.mysql.com/downloads/mysql/
   - 安装后验证：`mysql -u root -p`

3. **Redis** (>= 6.0)
   - Windows 下载：https://github.com/microsoftarchive/redis/releases
   - 或使用 Docker：`docker run -d -p 6379:6379 redis`
   - 验证：`redis-cli ping`（应返回 PONG）

### 可选软件

- Git：用于版本控制
- VS Code：推荐的开发工具

## 数据库配置

### 1. 创建数据库

```bash
# 登录 MySQL
mysql -u root -p

# 执行初始化脚本
source c:\AI-project\user_project\4213123\database\init.sql
```

或者直接执行：

```bash
mysql -u root -p < c:\AI-project\user_project\4213123\database\init.sql
```

### 2. 验证数据库

```bash
# 登录 MySQL
mysql -u root -p

# 选择数据库
USE h5_mall;

# 查看表
SHOW TABLES;

# 查看测试数据
SELECT * FROM products LIMIT 5;
```

## 后端配置

### 1. 安装依赖

```bash
cd c:\AI-project\user_project\4213123\backend
npm install
```

### 2. 配置环境变量

编辑 `backend/.env` 文件：

```env
# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=h5_mall

# Redis 配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# JWT 密钥（生产环境请修改）
JWT_SECRET=your-secret-key-change-in-production

# 服务端口
PORT=3000

# 日志级别
LOG_LEVEL=info
```

### 3. 启动后端服务

```bash
cd c:\AI-project\user_project\4213123\backend
npm start
```

开发模式（支持热重载）：

```bash
npm run dev
```

### 4. 验证后端服务

访问健康检查接口：

```bash
curl http://localhost:3000/api/health
```

或浏览器访问：http://localhost:3000/api/health

预期返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "ok"
  }
}
```

## 前端配置

### 1. 安装依赖

```bash
cd c:\AI-project\user_project\4213123\frontend
npm install
```

### 2. 启动前端服务

```bash
cd c:\AI-project\user_project\4213123\frontend
npm run dev
```

### 3. 访问前端

浏览器访问：http://localhost:3001

## 完整启动流程

### 方式一：手动启动（推荐用于开发）

1. **启动 Redis**
   ```bash
   redis-server
   ```

2. **启动后端**
   ```bash
   cd c:\AI-project\user_project\4213123\backend
   npm start
   ```

3. **启动前端**
   ```bash
   cd c:\AI-project\user_project\4213123\frontend
   npm run dev
   ```

### 方式二：使用脚本启动

创建 `start.bat` 文件：

```batch
@echo off
echo Starting H5 Mall...

echo Starting Redis...
start cmd /k redis-server

timeout /t 2

echo Starting Backend...
start cmd /k "cd backend && npm start"

timeout /t 3

echo Starting Frontend...
start cmd /k "cd frontend && npm run dev"

echo All services started!
echo Backend: http://localhost:3000
echo Frontend: http://localhost:3001
```

双击 `start.bat` 即可启动所有服务。

## 功能测试

### 1. 测试商品列表

访问：http://localhost:3001

应该能看到首页，包含：
- 搜索框
- 轮播图
- 商品分类
- 热门商品列表

### 2. 测试商品详情

点击任意商品，进入商品详情页，应该能看到：
- 商品图片轮播
- 商品价格
- 商品名称
- 商品描述
- 加入购物车按钮
- 立即购买按钮

### 3. 测试购物车

点击底部导航栏的"购物车"，应该能看到：
- 购物车商品列表
- 商品数量调整
- 总价计算
- 结算按钮

### 4. 测试用户中心

点击底部导航栏的"我的"，应该能看到：
- 用户头像和昵称
- 我的订单
- 我的收藏
- 收货地址
- 设置

### 5. 测试登录

点击用户中心的"退出登录"，然后重新登录：
- 输入手机号
- 获取验证码（模拟）
- 点击登录

## API 测试

### 使用 curl 测试

#### 获取商品列表

```bash
curl http://localhost:3000/api/products
```

#### 获取商品详情

```bash
curl http://localhost:3000/api/products/1
```

#### 用户注册

```bash
curl -X POST http://localhost:3000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"123456","nickname":"测试用户"}'
```

#### 用户登录

```bash
curl -X POST http://localhost:3000/api/users/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"123456"}'
```

### 使用 Postman 测试

1. 导入以下环境变量：
   ```
   base_url: http://localhost:3000/api
   ```

2. 创建请求：
   - GET {{base_url}}/products
   - GET {{base_url}}/products/1
   - POST {{base_url}}/users/register
   - POST {{base_url}}/users/login

## 常见问题

### 问题1：MySQL 连接失败

**错误信息**：`Access denied for user 'root'@'localhost'`

**解决方案**：
1. 检查 MySQL 密码是否正确
2. 修改 `backend/.env` 中的 `DB_PASSWORD`
3. 重启后端服务

### 问题2：Redis 连接失败

**错误信息**：`Redis connection failed`

**解决方案**：
1. 检查 Redis 是否启动：`redis-cli ping`
2. 检查 Redis 端口是否正确：`redis-cli -p 6379 ping`
3. 修改 `backend/.env` 中的 `REDIS_PORT`

### 问题3：端口被占用

**错误信息**：`EADDRINUSE: address already in use :::3000`

**解决方案**：
1. 查找占用端口的进程：
   ```bash
   netstat -ano | findstr :3000
   ```
2. 结束进程：
   ```bash
   taskkill /PID <进程ID> /F
   ```
3. 或修改 `backend/.env` 中的 `PORT`

### 问题4：前端无法连接后端

**错误信息**：`Network Error`

**解决方案**：
1. 检查后端是否正常运行
2. 检查 `frontend/vite.config.js` 中的代理配置
3. 清除浏览器缓存

### 问题5：npm install 失败

**错误信息**：`npm ERR! code ECONNREFUSED`

**解决方案**：
1. 切换 npm 源：
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```
2. 清除缓存：
   ```bash
   npm cache clean --force
   ```
3. 重新安装：
   ```bash
   npm install
   ```

## 生产环境部署

### 1. 构建前端

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist` 目录。

### 2. 配置 Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. 使用 PM2 管理后端进程

```bash
npm install -g pm2

cd backend
pm2 start src/app.js --name h5-mall-backend

# 查看状态
pm2 status

# 查看日志
pm2 logs h5-mall-backend

# 重启
pm2 restart h5-mall-backend
```

## 监控和日志

### 后端日志

日志文件位置：`backend/logs/`

- `error.log`：错误日志
- `combined.log`：所有日志

查看日志：

```bash
tail -f backend/logs/combined.log
```

### 前端调试

1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页的错误信息
3. 查看 Network 标签页的网络请求

## 性能优化建议

1. **前端优化**
   - 启用 CDN
   - 图片压缩
   - 代码分割
   - 懒加载

2. **后端优化**
   - 启用 Redis 缓存
   - 数据库索引优化
   - 使用连接池
   - 负载均衡

3. **数据库优化**
   - 定期备份
   - 慢查询优化
   - 读写分离

## 安全建议

1. **生产环境必须修改**
   - JWT_SECRET
   - 数据库密码
   - Redis 密码

2. **启用 HTTPS**
   - 使用 Let's Encrypt 免费证书
   - 配置 SSL 证书

3. **定期更新依赖**
   ```bash
   npm audit
   npm audit fix
   ```

## 技术支持

如有问题，请参考：
- 项目 README.md
- 开发规范.md
- 开发文档.md

祝使用愉快！
